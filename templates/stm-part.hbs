{{!-- dnd5e sheet part: the tab wrapper around the shared Psionics body. --}}
<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
  {{> "modules/stm/templates/stm-body.hbs" }}
</section>
